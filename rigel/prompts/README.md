# Prompts

One markdown file per prompt. First `# heading` becomes the title in the UI.

Example `fix-build.md`:

```markdown
# Fix the build

Run `swift build`. For each error, propose the minimal change. No drive-by refactors.
```